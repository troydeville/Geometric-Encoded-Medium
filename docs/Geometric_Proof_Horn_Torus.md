# Geometric Validation: Why the Horn Torus?

## Abstract
The GEM framework relies on a specific topological unit: the **Horn Torus**, defined by the major radius $R$ equaling the minor radius $r$ ($R=r$). This is not an arbitrary choice; it is the only topological shape that simultaneously satisfies six physical constraints derived from the model.

## 1. The Gravitational Scalar ($S$)
To derive Newton's $G$ from the Vacuum Impedance ($Z_0$), the required dimensionless scaling factor is:
$$S = \sqrt{2}\pi^{1/4} \approx 1.88279$$

**Geometric Match:**
In a Horn Torus, the diagonal distance from the center of the tube to the origin involves exactly $\sqrt{2}$ (due to the 1:1 aspect ratio) and the normalization of the circular cross-section ($\pi^{1/4}$). No other toroidal ratio ($R \neq r$) produces this constant naturally.

## 2. The Fine Structure Volume ($1/\alpha$)
We calculated the volume of a Horn Torus ($V = 2\pi^2 R^3$) using the shape constant $S$ as the radius.

* **Result:** $V \approx 131.746$
* **Match:** This corresponds to the inverse Fine Structure Constant ($1/\alpha \approx 137.036$) minus the Bohr Radius remainder ($5.29$).

**Implication:**
* **Ring Torus ($R > r$):** Volume is too small.
* **Spindle Torus ($r > R$):** Volume is undefined/complex.
* **Horn Torus ($R = r$):** Aligns with the electromagnetic capacity of the vacuum.

## 3. The Origin of Spin 1/2 (The Double Loop)
A spinor must rotate $720^\circ$ to return to its initial state.

**Geometric Match:**
The Horn Torus is the unique toroidal geometry where the **Villarceau Circles** (diagonal geodesics) intersect at exactly one point (the singularity). A particle following this geodesic must traverse the loop twice to complete a full circuit. The path length is exactly $4\pi S$.

## 4. The Vacuum Impedance Metric (The Factor 16)
Relationship between Vacuum Impedance ($Z_0$) and electron path length ($P = 4\pi S$):
$$Z_0 \approx 16 \times P$$

**Geometric Match:**
This allows the 1D path of the electron to map onto the 16 components of the 4D spacetime Metric Tensor.

## 5. The Proton Radius Anomaly (The Matter Gap)
The volumetric mismatch between the Horn Torus geometry and the total electromagnetic field is $\approx 3.86\%$.

**Geometric Match:**
This "error" predicts the shrinkage of the Proton Radius ($0.875 \to 0.841$ fm) when measured by a muon. The proton is not a solid sphere, but a Horn Torus where the "surface" is defined by impedance interaction depth.

## 6. The Singularity (Mass Definition)
**Geometric Match:**
The Horn Torus is the only torus with a **Single Point Singularity** ($R-r=0$).

**Implication:**
This allows vacuum impedance to transition from $377 \Omega$ (Surface) to $\approx 51 k\Omega$ (Core) at a specific point.
* **Ring Torus:** No center point.
* **Spindle Torus:** Volume core.
* **Horn Torus:** Point-mass singularity.